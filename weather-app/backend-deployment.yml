apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-deployment
  namespace: weather-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: weather-backend
  template:
    metadata:
      labels:
        app: weather-backend
    spec:
      containers:
        - name: weather-backend
          image: your-docker-username/weather-backend
          ports:
            - containerPort: 3001
---
apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: weather-app
spec:
  selector:
    app: weather-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3001
